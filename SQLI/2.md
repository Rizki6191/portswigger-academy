# Lab: SQL injection vulnerability allowing login bypass

Challenge Description:
 This lab contains a SQL injection vulnerability in the login function.

Goal:
 Perform a SQL injection attack that logs in to the application as the administrator user.

Solution:
 Use a payload that bypasses password verification by making the login condition always true and commenting out the rest of the query.

Payload:
```bash
'OR 1=1 --
```
