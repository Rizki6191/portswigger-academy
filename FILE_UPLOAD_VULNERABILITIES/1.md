Lab: Remote code execution via web shell upload

Challenge Description
 This lab contains a vulnerable image upload function. It doesn't perform any validation on the files users upload before storing them on the server's filesystem.

To solve the lab, upload a basic PHP web shell and use it to exfiltrate the contents of the file /home/carlos/secret. Submit this secret using the button provided in the lab banner.

You can log in to your own account using the following credentials: wiener:peter 

goal
get content of /home/carlos/secret via lfi

solver
diberikan sebuah web dimana bisa mengupload file untuk update avatar 
tujuannya untuk mendapatkan isi file /home/carlos/secret
untuk mendapatkannya bisa gunakan file_get_contents milik php.
`<?php echo file_get_contents('/home/carlos/secret'); ?>`
